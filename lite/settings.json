{
  "$schema": "https://claude.ai/schemas/claude-code-settings.json",
  "enableAllProjectMcpServers": false,
  "permissions": {
    "deny": [
      "Read ~/.ssh/**",
      "Read ~/.aws/**",
      "Read ~/.gnupg/**",
      "Read ~/.docker/config.json",
      "Read **/.env",
      "Read **/.env.*",
      "Read **/*.pem",
      "Read **/*.key",
      "Read ~/.bashrc",
      "Read ~/.bash_profile",
      "Read ~/.zshrc",
      "Read ~/.zprofile",
      "Read ~/.profile",
      "Read ~/.config/fish/config.fish",
      "Read ~/.config/fish/conf.d/**",
      "Read ~/.homebrew/**",
      "Read ~/.config/git/config.local",
      "Edit ~/.config/fish/config.fish",
      "Edit ~/.config/fish/conf.d/**",
      "Edit ~/.config/git/config",
      "Edit ~/.config/git/config.local",
      "Edit ~/.claude/settings.json",
      "Edit **/.env",
      "Edit **/.env.*"
    ]
  },
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(cat | jq -r \".tool_input.command // empty\"); if echo \"$CMD\" | grep -qEi \"rm\\s+-(r|f|rf|fr)\\s+(/|~|\\$HOME)\"; then echo \"BLOCKED: Destructive delete. Use trash or move to tmp instead.\" >&2; exit 2; fi'"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(cat | jq -r \".tool_input.command // empty\"); if echo \"$CMD\" | grep -qEi \"git\\s+push\\s+.*\\b(main|master|production)\\b\"; then echo \"BLOCKED: Direct push to protected branch. Use a feature branch and PR.\" >&2; exit 2; fi'"
          }
        ]
      },
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'CMD=$(cat | jq -r \".tool_input.command // empty\"); if echo \"$CMD\" | grep -qEi \"curl\\s.*\\|\\s*(bash|sh|zsh)|wget\\s.*\\|\\s*(bash|sh|zsh)\"; then echo \"BLOCKED: Pipe-to-shell detected. Download first, inspect, then execute.\" >&2; exit 2; fi'"
          }
        ]
      }
    ]
  }
}
